{
  "_id": "{{@ system @}}-{{@ datatype @}}-collect",
  "namespaced_identifiers": false,
  "source": {
    "operation": "{{@ datatype @}}-list",
    "system": "{{@ system @}}",
    "type": "rest"
  },
  "transform": {
    "rules": {
      "default": [
        [
          "rename",
          "{{@ datatype @}}.contact_id",
          "contact_id"
        ],
        [
          "rename",
          "{{@ datatype @}}.contact_id.name",
          "name"
        ],
        [
          "add",
          "_id",
          "_S.PrimaryKey"
        ]
      ]
    },
    "type": "dtl"
  },
  "type": "pipe"
}